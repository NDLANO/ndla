/**
 * Copyright (c) 2026-present, NDLA.
 *
 * This source code is licensed under the GPLv3 license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */

import { defineConfig } from "oxlint";
import baseConfig, { sharedIgnorePatterns } from "./packages/oxlint-config/src/index.ts";

export default defineConfig({
  extends: [baseConfig],
  options: {
    typeAware: true,
  },
  ignorePatterns: [
    ...sharedIgnorePatterns,
    // generated by `@ndla/types-backend`'s `generate` script
    "**/types-backend/src/**/*",
    "images/**/*",
  ],
  overrides: [
    {
      files: ["**/*.stories.{js,mjs,cjs,ts,jsx,tsx,mts,cts,mtsx,ctsx}"],
      rules: {
        "react/no-unescaped-entities": "off",
        "import-js/no-extraneous-dependencies": "off",
        "jsx-a11y/control-has-associated-label": "off",
      },
      plugins: ["react"],
    },
  ],
});
