# Root-level ignores only. Each subproject keeps its own .gitignore.

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Workspace dependencies and task-runner state
node_modules
.nx/cache
.nx/workspace-data
