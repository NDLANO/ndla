# AUTOGENERATED BY: modules/GithubWorkflowPlugin.scala (`./mill _.ghGenerate`)
# DO NOT EDIT MANUALLY.
name: 'CI: taxonomy-api'
on:
  workflow_dispatch:
    inputs: { }
  push:
    paths:
      - taxonomy-api/**
      - modules/**
      - build.mill
      - Dockerfile
  pull_request:
    paths:
      - taxonomy-api/**
      - modules/**
      - build.mill
      - Dockerfile
jobs:
  unit_tests:
    uses: ./.github/workflows/ci.yaml
    secrets: inherit
    with:
      subproject: taxonomy-api
