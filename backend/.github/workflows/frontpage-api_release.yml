# AUTOGENERATED BY: modules/GithubWorkflowPlugin.scala (`./mill _.ghGenerate`)
# DO NOT EDIT MANUALLY.
name: 'Release: frontpage-api'
on:
  workflow_dispatch:
    inputs: { }
  push:
    branches:
      - master
    paths:
      - frontpage-api/**
      - common/**
      - network/**
      - mapping/**
      - language/**
      - database/**
      - tapirtesting/**
      - scalatestsuite/**
      - modules/**
      - build.mill
      - Dockerfile
jobs:
  release:
    uses: ndlano/reusable-workflows/.github/workflows/release.yaml@main
    secrets: inherit
    with:
      component: frontpage-api
