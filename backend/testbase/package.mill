package build.testbase

import mill._, mill.scalalib._
import build.modules.{BaseModule, SharedDependencies}

object `package` extends BaseModule {
  override def moduleName                                       = "testbase"
  override def isComponent: Boolean                             = false
  override def dependencies(deps: SharedDependencies): Seq[Dep] = deps.logging ++ deps.testing ++ deps.database
  object test extends TestBase
}
